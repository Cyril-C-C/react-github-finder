import React, { Fragment } from 'react';
import Search from '../User/Search';
import Users from '../User/Users';

export const Home = () => (
  <Fragment>
    <Search />
    <Users />
  </Fragment>
);

export default Home;
